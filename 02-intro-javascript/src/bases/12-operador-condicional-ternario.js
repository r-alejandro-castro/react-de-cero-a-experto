const activo = true;

// let mensaje = '';

// if ( !activo) {
//     mensaje = 'Activo';
// } else {
//     mensaje = 'Inactivo';
// }

//Operador condicional ternario

const mensaje = ( !activo ) ? 'Activo' : 'Inactivo'

// NOTA: signo de exclamasion (!) significa negacion

// const mensaje = !activo && 'Activo';

//Es una forma corta de hacer un if

console.log(mensaje);