//Aqui se va a ejecutar codigo de javaScript antes de cada test

/*Lo que vamos a hacer es escribir un codigo que le permita a Node ejecutar el fecth de la API de giphy, ya que versiones antiguas de Node no soportan fetch
*/

//Instalamos la dependencia pnpm add -D whatwg-fetch

// import 'whatwg-fetch';